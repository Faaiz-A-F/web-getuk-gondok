"use client";

import { useContext } from "react";

// Replace with your actual context when created
export function useCart() {
  // const { cart } = useContext(CartContext);
  // return cart;

  return [];
}
